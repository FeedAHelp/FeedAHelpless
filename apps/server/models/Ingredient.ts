class Ingredient {
  id: number
  name: string
  imageName: string

  constructor(id: number, name: string, imageName: string) {
    this.id = id
    this.name = name
    this.imageName = imageName
  }
}

export default Ingredient
