class Ingredient {
  id: string
  name: string
  imageName: string

  constructor(id: string, name: string, imageName: string) {
    this.id = id
    this.name = name
    this.imageName = imageName
  }
}

export default Ingredient
