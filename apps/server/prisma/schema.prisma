// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id         String         @id @default(uuid())
  name       String
  image      String?
  register   Register       @relation(fields: [registerId], references: [id])
  registerId String         @unique
  logins     LoginHistory[]
}

model LoginHistory {
  id        Int      @id @default(autoincrement())
  loginTime DateTime @default(now())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
}

model Register {
  id          String @id @default(uuid())
  email       String @unique
  phone       String @unique
  password    String
  role        String
  accessToken String
  user        User?
  // contributions  contributionId[]   for production
}

model FeedahelpNewsletter {
  id       String   @id @default(uuid())
  userId   String?
  email    String   @unique
  createAt DateTime @default(now())
}

model FahCounter {
  id                 String @id @default(uuid())
  sectionName        String
  sectionDescription String
  imageIconUrl       String
  imageAlt           String
  value              String
  monthlyIncrease    String
}

model Ingredients {
  id        String @id @default(uuid())
  name      String
  imageName String
}

model contributionId {
  id        String        @id @default(uuid())
  contributor String
  type      ContributionType
  dateTime  DateTime
  value     Float
  createAt  DateTime      @default(now())
  // register  Register     @relation(fields: [contributor], references: [id]), we will use this later for production
}

enum ContributionType {
  financial
  volunteer
}
