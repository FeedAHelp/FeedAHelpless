import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  footerContainer: {
    alignItems: 'center',
    marginBottom: 10,
  },
  orText: {
    marginVertical: 5,
    color: '#999',
    fontSize: 14,
  },
});

export default styles;
